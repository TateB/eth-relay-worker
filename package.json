{
	"name": "eth-relay-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"generate-api-keys": "bun run ./scripts/generate-api-keys.ts",
		"generate-private-key": "bun run ./scripts/generate-private-key.ts",
		"get-address": "bun run ./scripts/get-address.ts"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.4.5",
		"@cloudflare/workers-types": "^4.20240712.0",
		"clipboardy": "^4.0.0",
		"generate-api-key": "^1.0.2",
		"typescript": "^5.5.2",
		"vitest": "1.5.0",
		"wrangler": "^3.60.3"
	}
}
